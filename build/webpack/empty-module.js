// Stub for @a0/auth0-extension-ui barrel deps the app never uses
// (CodeEditor -> codemirror/react-codemirror2, DragAndDrop -> react-dropzone).
// The barrel statically imports these, but those components are unused here,
// so their bindings are never invoked at runtime.
module.exports = {};
